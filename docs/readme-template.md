# Project Name

## Purpose

Коротко описати, для чого існує утиліта.

## Requirements

- OS
- compiler/interpreter
- dependencies

## Build

Як зібрати програму.

## Run

Як запустити:

```bash
./app [options]
```

## Parameters

- `--help` — показати довідку.
- `--input <file>` — вказати вхідний файл.

## Exit codes

- `0` — команда виконана успішно.
- `1` — помилка аргументів або виконання.

## Example

Приклад команди та очікуваного виводу:

```text
$ ./app --input input.txt
Processed input.txt
```

## Limitations

Опишіть підтримувані формати, обмеження розміру вхідних даних та відомі проблеми.
